# A题“药材的烘干问题”题目分析报告

## 1. 材料完整性与任务边界

当前目录包含题面 `A题.pdf`、烘房环境数据 `附件1.xlsx`、药材半径数据 `附件2.xlsx`，以及 `result1.xlsx`—`result4.xlsx` 四个结果模板。题目共四问，前一问的建模选择会直接影响后一问：问题1建立固定物性、固定半径的预热模型；问题2引入随温度和含水率变化的物性；问题3把问题2延长到达到干燥阈值；问题4再引入半径随时间收缩的移动边界。

题目没有要求区分药材轴向位置，只要求“到药材中心的距离”方向上的结果。圆柱长 25 cm、半径 2 cm，长径比为 6.25，圆柱两端面积与侧面积之比为 0.08。结合输出变量的径向表述，可忽略端部附近的轴向效应，把主体区域近似为轴对称的一维径向传热传质问题。

## 2. 数据核对

### 2.1 烘房边界数据

附件1共有 241 个观测时刻，时间范围为 0—14400 s，步长 60 s。烘房温度由 28°C 上升并稳定在约 50°C；烘房水分浓度由 0.01963 kg/kg 上升并稳定在约 0.05 kg/kg。关键观测如下。

| 时间/h | 温度/°C | 水分浓度/(kg/kg) |
|---:|---:|---:|
| 0.0 | 28.0000 | 0.01963 |
| 0.5 | 41.5130 | 0.03307 |
| 1.0 | 47.4850 | 0.04272 |
| 2.0 | 49.8520 | 0.04894 |
| 3.0 | 50.1950 | 0.04977 |
| 4.0 | 50.1650 | 0.04986 |

3—4 h 数据的均值分别为 49.9989°C 和 0.04999 kg/kg，标准差分别为 0.1531°C 和 0.00015 kg/kg。这说明 4 h 以后采用恒温恒湿边界 50°C、0.05 kg/kg 有明确的数据依据。0—4 h 内使用保形分段三次插值（PCHIP）生成连续边界，既保留实测波动，又避免普通高阶样条产生非物理超调。

### 2.2 半径数据

附件2共有 145 个观测时刻，时间范围为 0—259200 s（72 h），步长 1800 s。半径由 2.000 cm 单调缩小至 1.198 cm，其中 6、12、24、72 h 的半径分别为 1.374、1.248、1.204、1.198 cm。半径数据没有回升点，后期逐渐进入平台。问题4中对半径同样采用单调 PCHIP 插值，禁止使用可能在节点间产生回弹或负半径的普通三次样条。

### 2.3 结果模板映射

原模板只用“…”示意中间行列，正式结果必须展开完整网格并另存，不能覆盖附件3中的原模板。

| 文件 | 时间网格 | 空间网格 | 工作表 |
|---|---|---|---|
| result1.xlsx | 1—1800 s，步长1 s | 0—2 cm，步长0.1 cm | 温度、水分浓度 |
| result2.xlsx | 1—10800 s，步长1 s | 0—2 cm，步长0.1 cm | 温度、水分浓度 |
| result3.xlsx | 60 s至烘干结束，步长60 s | 0—2 cm，步长0.1 cm | Sheet1 |
| result4.xlsx | 60 s至烘干结束，步长60 s | 固定内部位置加“药材表面”列 | Sheet1 |

问题4的半径会缩小，固定距离列不能越过任一时刻的药材表面。待求得结束时刻后，以该时刻半径为下限，将固定列设置为 0、0.1、…、不超过最小半径的最大 0.1 cm 网格点，并额外保留“药材表面”列。若结束时半径约为 1.2 cm，则固定列应为 0—1.1 cm，表面值单独给出。

## 3. 题面物理事实与建模红线

1. 药材是有内部空间分布的圆柱体，Biot 数不能支持集总参数近似。问题1的热扩散率为
   \[
   \alpha=\frac{k}{\rho c_p}=1.6886\times10^{-7}\ \mathrm{m^2/s},
   \]
   热 Biot 数为 \(Bi_T=hR/k=1.3889\)，明显大于 0.1，必须求解内部温度场。
2. 初始药材含水率为 2.55 kg/kg，是干基含水率；不得擅自换成湿基含水率，也不得把附件1中的环境水分浓度解释为相对湿度。
3. 问题1在初始含水率处的扩散系数约为 \(4.94\times10^{-9}\ \mathrm{m^2/s}\)，对应传质 Biot 数约为 3.24，内部浓度梯度同样不能忽略。
4. 问题2—4的扩散系数含绝对温度 \(T\)，代入经验式前必须把摄氏温度转换为 K；热方程展示和结果文件仍使用 °C。
5. 物性参数随 \(C\) 和 \(T\) 变化时，应在每个空间节点和每个时间步更新；把物性固定在初值会系统性改变后期干燥速度。
6. “药材各处低于 0.15 kg/kg”意味着停止条件是 \(\max_r C(r,t)<0.15\)，不能只检查表面。一般中心最慢，但程序仍需逐时检查整个剖面。
7. 问题4的空间域随半径变化。不能只把边界节点向内移动而仍沿用固定半径离散系数，必须进行移动坐标变换或采用真正的动网格守恒离散。

## 4. 建议模型

### 4.1 固定半径下的温度场

取径向坐标 \(r\in[0,R]\)，温度满足

\[
\rho(C)c_p(C)\frac{\partial T}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}
\left(rk(C)\frac{\partial T}{\partial r}\right).
\]

初始条件和边界条件为

\[
T(r,0)=28^\circ\mathrm C,\qquad
\left.\frac{\partial T}{\partial r}\right|_{r=0}=0,
\]

\[
\left.k(C)\frac{\partial T}{\partial r}\right|_{r=R}
=h\,[T_\infty(t)-T(R,t)].
\]

问题1采用题面给出的常数 \(\rho,c_p,k\)；问题2—4采用相应附录中的 \(C\) 相关经验式。题面没有给出蒸发潜热或热湿交叉项，因此不额外虚构潜热源项；温度通过扩散系数 \(D(C,T)\) 影响水分迁移，含水率通过 \(\rho,c_p,k\) 反向影响温度场。

### 4.2 固定半径下的水分场

水分浓度满足非线性扩散方程

\[
\frac{\partial C}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}
\left(rD(C,T)\frac{\partial C}{\partial r}\right),
\]

其中

\[
C(r,0)=2.55,\qquad
\left.\frac{\partial C}{\partial r}\right|_{r=0}=0,
\]

\[
\left.D(C,T)\frac{\partial C}{\partial r}\right|_{r=R}
=h_m[C_\infty(t)-C(R,t)].
\]

问题1使用 \(D=7\times10^{-9}\exp(-0.89/C)\)；问题2和问题3使用

\[
D=2.4\times10^{-3}\exp(-0.45/C)\exp(-3850/T_K).
\]

### 4.3 收缩半径下的固定域变换

问题4令 \(\xi=r/R(t)\in[0,1]\)。若含水率作为随材料点变化的干基状态量，径向收缩速度为 \(u_r=r\dot R/R\)，物理坐标中的对流项经材料坐标变换后抵消，水分方程写成

\[
\left.\frac{\partial C}{\partial t}\right|_{\xi}
=\frac{1}{R(t)^2\xi}\frac{\partial}{\partial\xi}
\left(\xi D(C,T)\frac{\partial C}{\partial\xi}\right).
\]

表面传质条件相应变为

\[
\left.\frac{D}{R(t)}\frac{\partial C}{\partial\xi}\right|_{\xi=1}
=h_m[C_\infty(t)-C(1,t)].
\]

温度方程作同样变换，并使用附录4给出的 \(\rho,c_p,k,D\)。这种写法同时体现了扩散距离缩短和表面积变化，且计算域始终固定，便于守恒离散。

## 5. 数值求解方案

空间上采用轴对称有限体积法。控制体体积权重按环形面积计算，中心控制体直接使用零通量对称条件，表面控制体直接写入 Robin 对流通量；界面处的 \(k\) 和 \(D\) 使用调和平均，避免物性变化时通量不连续。时间上把离散后的方程组交给隐式 BDF 求解器，输出时再插值到题目指定的时间和空间网格。

基准网格建议取 101 个径向节点，并使用 51、101、201 节点做网格收敛检验。时间积分容差至少比较 \(10^{-6}\) 与 \(10^{-8}\) 两档。烘干结束时刻采用连续事件定位，同时用 60 s 输出网格上首次全域低于 0.15 的时刻交叉核对。

## 6. 分问求解链

### 问题1

用附件1的 0—1800 s 环境曲线作为时变 Robin 边界，求解常物性热方程和含浓度相关扩散系数的水分方程。先输出题目指定的 7×5 温度表与水分表，再生成 1 s×0.1 cm 的完整结果。应检查中心梯度为零、表面变化最快、温度不超出环境上界、水分总量持续下降。

### 问题2

沿用相同几何与边界，把 \(\rho,c_p,k,D\) 改为附录3经验式并逐节点更新。0—3 h 完全落在附件1数据范围内，不需要外推。输出 0.5 h 间隔的论文表格和 1 s 间隔的完整结果。

### 问题3

沿用问题2模型。0—4 h 使用附件1插值，4 h 后使用 50°C、0.05 kg/kg 的恒定边界。以全空间最大含水率低于 0.15 为结束判据，输出每 6 h 的论文表和每 60 s 的完整结果。附件1末段统计值可作为恒温边界选择的独立证据。

### 问题4

用附件2构造 \(R(t)\)，采用固定域变量 \(\xi\) 求解移动边界模型，物性改用附录4经验式。每个输出时刻把 \(C(\xi,t)\) 重新映射到实际距离 \(r=\xi R(t)\)。结束判据仍为全域 \(C<0.15\)，并与问题3比较收缩导致的扩散路径变化。

## 7. 必做验证

1. **单位检验**：热方程、水分方程和两类 Robin 边界逐项量纲一致；Arrhenius 公式只用 K。
2. **守恒检验**：控制体内总含水量变化与表面传质通量积分相符；热量变化与表面对流热通量相符。
3. **网格与容差检验**：加密网格、收紧容差后，关键表格四位小数和结束时刻保持稳定。
4. **独立实现检验**：用有限差分或恒物性圆柱解析级数复核一个简化算例，避免单一程序的符号和边界错误。
5. **边界极限检验**：令换热/传质系数趋近零时场量接近初值；令系数增大时表面更快逼近环境值。
6. **物理趋势检验**：温度由表面向中心升高，含水率由表面向中心降低；中心应是最后达到 0.15 的位置。若出现反向梯度或明显超调，优先检查通量符号和插值。
7. **问题4几何检验**：所有固定输出位置都满足 \(r<R(t)\)，表面列必须由 \(\xi=1\) 单独计算。

## 8. 当前风险与待求解后裁定项

- 题面未给出潜热和湿度换算关系，因此采用题面参数能够闭合的热传导—水分扩散模型，不自行增加不可标定参数。
- result4 的固定距离列数需在得到结束时刻及最小半径后最终确定；原模板中的“药材表面”列必须保留。
- 若问题4的结束时刻超过附件2的 72 h 数据范围，不直接外推收缩曲线；应先做平台半径假设的敏感性分析，并在论文中明确边界。题面称烘干一般持续 2—3 天，附件覆盖 72 h，预期数据范围足以覆盖结束时刻。
- 题目要求四位小数，但内部计算至少保留双精度，最后写表时统一四舍五入，防止逐步舍入累积误差。

